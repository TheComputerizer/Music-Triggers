apply from: rootProject.file('gradle\\local.gradle') //TODO Add another script & flag to switch between local/external builds

dependencies {
    compileOnly("mods.thecomputerizer.theimpossiblelibrary:theimpossiblelibrary:${til_version}")
}