repositories {
    maven {
        name = 'Modrinth'
        url = uri 'https://api.modrinth.com/maven'
        content {
            includeGroup 'maven.modrinth'
        }
    }
}

dependencies {
    compileOnly "maven.modrinth:the-impossible-library:$til_version"
    //compileOnly "maven.modrinth:the-impossible-library:${til_version}:javadoc"
    //compileOnly "maven.modrinth:the-impossible-library:${til_version}:sources"
}