dependencies {
    implementation(project(':forge'))
}

jar {
    mustRunAfter(':forge:jar')
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
    from(configurations.embed.collect { it.isDirectory() ? it : zipTree(it) })
}