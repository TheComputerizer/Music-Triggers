A Basic Trigger is a special type of Simple Trigger that has specific activation conditions which make it unable to be
used in trigger combinations. Basic triggers do not have unique parameters and are not affected by the priority system.

The **3** basic triggers are `generic`, `loading`, and `menu`.