pluginManagement {
    repositories {
        gradlePluginPortal() {
            content {
                settings_plugin_content.split(';').each {grp ->
                    includeGroup grp
                }
            }
        }
        maven {
            name = 'WagYourMaven Releases'
            url = uri 'https://maven.wagyourtail.xyz/releases'
            content {
                unimined_releases.split(';').each {grp ->
                    includeGroup grp
                }
            }
        }
        maven {
            name = 'WagYourMaven Snapshots'
            url = uri 'https://maven.wagyourtail.xyz/snapshots'
            content {
                unimined_snapshots.split(';').each {grp ->
                    includeGroup grp
                }
            }
        }
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '+'
}

rootProject.name = 'Music-Triggers'

def paths = [ ':api', ':legacy:cleanroom' ]

paths.forEach {
    include it
}

paths.forEach {
    String[] split = (it.startsWith(':') ? it.substring(1) : it).split(':')
    findProject(it)?.name = split.length>1 ? split.last() : split.first()
}